# Secrets - private ntfy topic and site access code stay out of the public repo
ntfy_topic.txt
site_passcode.txt
firebase_url.txt

# Python
__pycache__/
*.pyc
.venv/

# Your personal studio data (plans, done marks) - stays on your PC
plans.db

# Node (only needed locally to run smoke_test.js)
node_modules/
package.json
package-lock.json

# NewsData.io API key (local only; cloud uses the GitHub Secret)
newsdata_key.txt
